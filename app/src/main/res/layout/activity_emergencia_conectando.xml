<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/background_gradient">

    <!-- Contenido central -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical"
        android:gravity="center"
        android:padding="32dp">

        <!-- Icono animado (placeholder) -->
        <View
            android:id="@+id/iconoPulsante"
            android:layout_width="120dp"
            android:layout_height="120dp"
            android:background="@drawable/diamond_shape_red"
            android:layout_marginBottom="40dp" />

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Conectando con médico..."
            android:textSize="22sp"
            android:textStyle="bold"
            android:textColor="#E53935"
            android:gravity="center"
            android:layout_marginBottom="16dp" />

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Por favor espera, estamos buscando al médico de guardia disponible más cercano."
            android:textSize="16sp"
            android:textColor="@color/text_gray"
            android:gravity="center"
            android:layout_marginBottom="40dp" />

        <!-- Barra de progreso simple -->
        <ProgressBar
            android:layout_width="48dp"
            android:layout_height="48dp"
            android:indeterminate="true"
            android:layout_marginBottom="24dp" />

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Tiempo estimado: menos de 2 minutos"
            android:textSize="14sp"
            android:textColor="@color/text_gray"
            android:gravity="center" />

    </LinearLayout>

    <!-- Botón cancelar -->
    <TextView
        android:id="@+id/btnCancelarConexion"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:layout_centerHorizontal="true"
        android:layout_marginBottom="40dp"
        android:text="Cancelar"
        android:textSize="16sp"
        android:textColor="#E53935"
        android:padding="16dp" />

</RelativeLayout>

